import { html } from 'lit';
import '../components/button/atomic.button.component';

export default {
  title: 'AtomicCardComponent',
};

const Template = () => html`
  <atomic-card></atomic-button>
`;

export const Interactive = Template.bind({});
Interactive.args = {};

